# Each piece of content has YAML front matter with these properties:

title:
  type: String
  required: true

description:
  type: String

class:
  type: String

toc:
  type: Hash

order:
  type: Integer

image: {}
